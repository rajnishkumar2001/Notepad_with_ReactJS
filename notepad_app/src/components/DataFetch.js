// import jwt from 'jwt-decode'

function DataFetch(data) {
    const value = Object.keys(data)
    if ("email" === value[0] && "password" === value[1] && "remember" === value[2]) {
        
    }
    if("email" === value[0] && "username"===value[1] && "password" === value[2]){
        
    }
}

export default DataFetch